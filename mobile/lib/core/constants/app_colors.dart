import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF00A36C);
  static const Color primaryDark = Color(0xFF008E5D);
  static const Color primaryLight = Color(0xFFE5F6F0);
  
  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF8F9FB);
  static const Color surface = Colors.white;
  static const Color cardShadow = Color(0x08000000); // 3% opacity black
  
  // Typography
  static const Color textPrimary = Color(0xFF0A191E);
  static const Color textSecondary = Colors.grey;
  static const Color textLight = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Colors.orange;
  static const Color info = Colors.blue;
}
